import { useState } from 'react';

const styles = {
    // TODO: Add inline CSS
};

function Contact() {
    // TODO: Contact Form with fields for a name, an email address, and a message
    // TODO: Notification that field is required when required field is missing
    // TODO: Set up email validator and create notification when invalid email is entered
    return <h1>Contact Me</h1>
}

export default Contact;